services:
  contentforge:
    build: ./backend
    container_name: contentforge
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/storage
